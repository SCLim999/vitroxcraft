# VitroxCraft 使用说明书

**ViTrox Campus 2.0 · 网页版 Minecraft 教育世界**

> 以 [CYC Architect 设计的 ViTrox Campus 2.0](https://www.cycarch.com/vitrox-campus-20)（马来西亚槟城·峇都交湾）为蓝本的 3D 方块世界。
> 在游戏中漫步园区、阅读建筑设计知识、自由建造，还可以和同学多人连线一起探索。

> 🌐 **界面语言**：游戏支持 **中文 / English / Bahasa Melayu** 三种语言——在开始菜单右上角点击切换，
> 界面、提示、方块名称和全部 6 个建筑知识讲解都会随之切换；选择会自动记住，首次打开按浏览器语言自动选择。

---

## 目录

1. [系统需求](#1-系统需求)
2. [如何开启](#2-如何开启)
3. [操作方法](#3-操作方法)
4. [玩法一：建筑探索任务（10 个知识点）](#4-玩法一建筑探索任务)
5. [玩法二：自由建造](#5-玩法二自由建造)
6. [玩法三：CodingMine 编程小任务](#6-玩法三codingmine-编程小任务)
7. [多人连线](#7-多人连线)
8. [存档与重置世界](#8-存档与重置世界)
9. [常见问题（FAQ）](#9-常见问题faq)
10. [给老师的课堂建议](#10-给老师的课堂建议)
11. [技术说明](#11-技术说明)

---

## 1. 系统需求

| 项目 | 要求 |
|---|---|
| 设备 | 电脑（键盘鼠标，体验最佳）或**手机/平板（触屏操作）** |
| 浏览器 | Chrome、Edge、Firefox、Safari 的近年版本 |
| 网络 | 打开页面时需要联网（加载 Three.js 图形库和多人组件）；单人游玩加载完成后即可离线 |
| 安装 | **无需安装任何东西** —— 就是一个网页 |

---

## 2. 如何开启

### 方式 A：在线访问（推荐，适合发给学生）

仓库合并到 `main` 并开启 GitHub Pages 后，直接访问：

```
https://sclim999.github.io/pps2114-quiz/minecraft/
```

> 开启 Pages 的方法：仓库 **Settings → Pages** → Source 选 **Deploy from a branch** → 分支选 `main`、目录选 `/ (root)` → Save，等一两分钟即可。

### 方式 B：本地打开

1. 下载 `minecraft/index.html` 这一个文件（GitHub 页面上点 **Download raw file**）；
2. 双击用浏览器打开即可。

进入页面后，点绿色的 **「▶ 进入园区」** 按钮开始。浏览器会锁定鼠标指针（这是正常的，按 `Esc` 可随时退出）。

---

## 3. 操作方法

| 按键 / 操作 | 功能 |
|---|---|
| `W` `A` `S` `D` | 前 / 左 / 后 / 右移动 |
| 移动鼠标 | 转动视角 |
| `空格` | 跳跃（上台阶、上坡道时会用到） |
| `F` | 切换**飞行模式**（空格上升、`Shift` 下降）——想俯瞰全景或上屋顶时非常好用 |
| 鼠标**左键** | 破坏方块 |
| 鼠标**右键** | 放置当前选中的方块 |
| `1`–`9` 或 滚轮 | 切换物品栏方块 |
| `E` | 靠近金色信息方块时：阅读建筑知识；弹窗打开时：关闭弹窗 |
| `Q` | 显示 / 隐藏任务清单 |
| 弹窗里的 🔊 按钮 | 朗读当前这段讲解/对话（再点一下或按 ⏹ 可停止） |
| `M` | 开关音效 |
| `Esc` | 解锁鼠标，回到菜单（再点「进入园区」继续） |

左上角显示你的坐标，下方是**小地图**——固定比例的园区俯视图，圆圈是中央庭院方位参照，金色/绿色小点是未探索/已探索的知识点，浅蓝小点是 NPC，白色三角箭头是你自己（会随转身方向旋转），方便随时确认自己在园区哪个角落、该往哪边走。顶部显示探索进度、**⏱ 游玩计时**（只在实际操控角色时累计，开菜单或看弹窗时会暂停）和在线人数。

**📱 手机 / 平板（触屏操作）**：点「进入园区」后自动切换触屏模式——**左侧摇杆**移动、**右侧空白处滑动**转视角，右下角按钮：`✈` 飞行开关、`⬆` 跳跃/上升、`⬇` 下降（飞行时）、`⛏` 挖方块（按住连挖）、`🧱` 放方块、`💬` 对话/阅读信息点；右上角 `☰` 返回菜单。建议**横屏**游玩。

---

## 4. 玩法一：建筑探索任务

园区里藏着 **10 个金色信息方块**（上方有蓝色 ⓘ 标记漂浮）。走到方块附近（约 3 格内），屏幕下方会出现提示，按 `E` 即可阅读该处的建筑设计讲解（中英对照标题）。按 `Q` 可随时查看还有哪些没找到。

| # | 知识点 | 在哪里找 |
|---|---|---|
| ① | 中央圆形庭院 | 园区正中心，水池旁 |
| ② | 无门的入口 | 南面大门外的广场（出生点沿步道直走） |
| ③ | 环形景观坡道 | 入口西侧的草坪上（讲解设计概念） |
| ④ | 绿色屋顶花园 | 环形楼屋顶——走**庭院西侧深灰色楼梯塔**（内部楼梯直通屋顶花园） |
| ⑤ | 几何体量的碰撞 | 东侧白色大楼（VITROX 招牌楼）西南角外 |
| ⑥ | 工业 × 人文校园 | VITROX 大楼内部一层（从面向庭院的开口进入） |
| ⑦ | Campus 3.0 与鱼池庭院 | 沿东侧马路走到新园区，鱼池木桥南端 |
| ⑧ | 启发 ViTrox 的相机 | ViTrox Gallery 走廊中段的金色展台（玻璃罩里的相机） |
| ⑨ | ViTrox Education 品牌重塑 | Campus 3.0 东侧的白色飘带大楼入口（讲师 Dr Janaka Low 所在处） |
| ⑩ | 电脑室 · CodingMine 编程课堂 | VITROX 招牌大楼四楼电脑室，两室分隔墙的大黑板旁 |

**🔊 文字转语音（Text-to-Speech）**：每个知识点和 NPC 对话弹窗右下角都有一个朗读按钮，点一下就用浏览器内建的语音朗读当前显示的文字（跟随所选语言：中文/English/Bahasa Melayu）。也可以在开始菜单勾选「自动朗读讲解内容」，之后每次打开新的知识点或对话都会自动开始朗读——适合朗读能力较弱的学生，或当作背景讲解使用。此功能不需要联网、不需要账号，由浏览器自带的语音引擎驱动，语音音色因浏览器/系统而异；如果浏览器不支持，朗读按钮和开关会自动隐藏。

**🚻 男声 / 女声**：NPC 对话会自动按角色造型选择声音——男性角色（保安、工程师、创办人等）用男声，女性角色（Siti、Mei Ling、Priya 等）用女声。建筑知识点讲解没有固定角色，可以在开始菜单的「讲解旁白声音」处选择 👨 或 👩，之后所有知识点朗读都会用你选的声音。声音的实际音色取决于你电脑/浏览器安装了哪些语音包（Windows/Edge 通常自带中英文男女声，Chrome 在部分系统上语音选择较少）。

正门进来的门厅还有一座白色接待柜台（Siti 就站在柜台后）和一座横跨门厅上空的二楼玻璃连桥（垂吊绿植 + Welcome 横幅），连接环形楼两侧的二层。

VITROX 招牌大楼**四楼**东半层有**两间电脑室**，共约 20 台电脑（书桌 + 显示器 + 主机）；从食堂旁的蓝色地毯走进楼梯间，沿直跑楼梯一路跳上四楼即可到达（记得边走边按空格跳上台阶）。

集齐 10 个会有通关祝贺。每个知识点都对应真实建筑的设计概念：放射状布局、实与虚、旋转动感、绿色建筑、几何并置、工业与校园文化的融合，以及电脑室里的编程教育。

园区里还住着 **20 位 NPC 人物**（保安、接待员、工程师、园丁、厨师、学生、职员、建筑师、讲师、市场专员），分布在大门、庭院、屋顶、食堂、球场、停车场、Campus 3.0 鱼池边、ViTrox Education 和四楼电脑室。走近时他们会转身看你，按 `E` 可以交谈——对白也支持三种语言，还会给你指路。

---

## 5. 玩法二：自由建造

物品栏提供 9 种方块：**草方块、石头、白色混凝土、玻璃幕墙、木板、原木、树叶、ViTrox 蓝、太阳能板**——正是搭建这座建筑所用的「材料」。你可以：

- 给园区加盖新楼、天桥、雕塑；
- 用「ViTrox 蓝」在墙上拼字；
- 拆掉一面墙看看楼板和柱网结构（金色信息方块不可破坏）。

你的所有改动会自动保存在本机浏览器里（详见第 8 节）。

---

## 6. 玩法三：CodingMine 编程小任务

四楼两间电脑室里，**每间电脑室第一排的 5 台电脑屏幕是绿色的**（其余都是蓝色办公屏），头顶还悬浮着 **Station 1～Station 10** 编号牌，一眼就能认出——那是虚构软件公司 **CodingMine（编码矿业）** 的工作站。**每一台电脑固定对应一章**：Station 1 是第一章，Station 2 是第二章……一路到 Station 10 是最后一章，走近哪一台按 `E`，就直接开始那一章的练习，所有例题都用 **C/C++** 撰写：

| 工位 | 章节 | 学习点 | 游戏任务 |
|---|---|---|---|
| Station 1 | 第一章 · All that Syntax | 算法、计算机语言、C/C++ 字符串语法 | 用 `player.say(...)` 帮角色打招呼、帮 CEO 修 Bug |
| Station 2 | 第二章 · Location, Location, Location | 三维坐标（X/Y/Z 轴）与相对坐标 | 批量安装天花板吊灯 |
| Station 3 | 第三章 · A Varied Outcome | 变量声明、数据类型、变量更新 | 农场自动化：种植、分类、销售 |
| Station 4 | 第四章 · Animals Are Friends | 列表索引（从 0 开始）、append/sort/reverse | 动物医院：排序、归类、投喂 |
| Station 5 | 第五章 · Agent and Loops | for 循环、大括号 { } 区块 | 操控 Agent 机器人做家务 |
| Station 6 | 第六章 · Driving Around | if-else 条件判断、布尔值 true/false | 教 Agent 依交通标志「开车」 |
| Station 7 | 第七章 · Emergency Response | while 循环 | 灾害模拟：自动搭建防洪堤、重建地基 |
| Station 8 | 第八章 · Functions | 函数的定义与呼叫 | 封装重复动作，整理大型项目代码 |
| Station 9 | 第九章 · All Fun and Games | 问题拆解（Decomposition） | 从零做出控制器 + 发射机制 + 计分板的小游戏 |
| Station 10 | 第十章 · 课程毕业项目与创意挑战 | 综合运用、除错心态 | 开放式沙盒挑战 |

（电脑室 1 的 Station 1～5 在南边，电脑室 2 的 Station 6～10 在北边——沿楼梯上到四楼后往北走就会遇到。）每道题选对即可获得分数（首次答对才计分，重做不会重复加分），答错会显示正确答案和解释，可以直接重试。右上角一直显示 **💻 CodingMine 分数**。分数保存在本机浏览器里，换设备不会带过去。

两间电脑室之间的分隔墙上挂着一块**很大的黑板**（两面都看得到），实时列出全部 10 章的完成进度和总分，跟右上角的分数即时同步——这就是给玩家看的成绩仪表盘（dashboard），相当于教室里的公开成绩榜。这块黑板也是**探索任务的第 ⑩ 个知识点**，走近它按 `E` 即可了解这间电脑室和 CodingMine 课程的介绍；按 `Q` 打开的任务列表只列建筑探索进度，不重复显示章节明细。

两间电脑室里还有 **4 位 Computing School 的核心人物兼讲师**，走近他们按 `E` 可以交谈：

| 讲师 | 教授科目 |
|---|---|
| **Ts Dr Lim SC**（Programme Leader） | 软件工程（Software Engineering） |
| **Ms Syira** | 数据科学（Data Science） |
| **Ms Khor JY** | 移动应用开发 · Flutter（Mobile App Dev） |
| **Mr Eng YK** | 人工智能（Artificial Intelligence） |

ViTrox Education 学院大楼底层的架空柱廊广场里（不在电脑室、也不挨着 Dr Janaka Low，五台之间也彼此拉开距离，分散在大楼各处）还有 **5 台木箱造型的站立式信息机**——木柱 + 木箱 + 彩色图标牌，样子像 Minecraft 里常见的邮箱/告示牌，各自介绍 ViTrox College 一个不同的真实文凭/学士课程，内容都写得简短，走近按 `E` 即可阅读：

| 图标 | 课程 |
|---|---|
| 💻 蓝色 | Diploma in Computer Science |
| 📈 绿色 | Diploma in Business Studies |
| ⚙️ 红色 | Diploma in Mechatronics Engineering |
| 🎓 紫色 | Bachelor of Mechatronics Engineering (Hons) — 与 UCSI University 合作 |
| ⚡ 黄色 | Diploma in Electrical and Electronic Engineering |

市场专员 **Cindy** 就在这层楼附近走动（不是固定站着），负责介绍这几台信息机；走近她按 `E` 可以交谈。

---

## 7. 多人连线

无需任何服务器，基于浏览器点对点连接（WebRTC），适合课堂上十几人同时游玩。

### 房主（老师/发起人）

1. 在开始菜单填好**你的名字**；
2. 点 **「创建多人房间」**，几秒后会显示一个 **5 位房间代码**（如 `AB3CD`）；
3. 把代码告诉同学；
4. 点「进入园区」开始玩。**房主的页面必须保持打开**——房主就是服务器。

### 加入者（学生）

1. 打开同一个网页，填好**你的名字**；
2. 在「房间代码」框输入房主给的 5 位代码，点 **「加入房间」**；
3. 看到「✅ 已加入房间」后，点「进入园区」。

### 连线后

- 每个人都是一个方块小人，头顶有名字牌，位置实时同步；
- 任何人挖 / 放方块，所有人都会看到；
- 顶部显示当前在线人数和房间代码；
- 有人加入或离开会有提示。

### 多人模式须知

| 事项 | 说明 |
|---|---|
| 世界归属 | 世界以**房主**的存档为准；加入者的改动只存在房主那里，不会写进自己浏览器 |
| 重置世界 | 只有房主可以重置，重置会同步给所有人 |
| 房主关页面 | 所有人断线（世界仍保留在房主浏览器的存档中，重开房间即可继续） |
| 探索进度 | 每人独立计算，互不影响 |
| 人数建议 | 建议 ≤ 15 人一个房间；人多可开多个房间 |
| 连不上？ | 多人连线使用公共 PeerJS 云做「牵线」，个别校园网防火墙可能拦截。可换网络（如手机热点）重试 |

> **进阶**：学校若想自建信令服务器（如内网使用），可在网址后加参数指定：
> `index.html?peerhost=服务器IP&peerport=9000&peerpath=/`（服务器端运行开源的 [peerjs-server](https://github.com/peers/peerjs-server) 即可）。

---

## 8. 存档与重置世界

- **自动存档**：你放置/破坏的每一个方块都会自动记录到本机浏览器的 `localStorage`，下次打开同一浏览器会自动恢复。
- **换浏览器/换电脑不带存档**；清除浏览器数据会清掉存档。
- **重置世界**：按 `Esc` 回菜单 → 点「🔄 重置世界」→ 确认后恢复原始园区（多人模式下仅房主可用）。

---

## 9. 常见问题（FAQ）

**打开后一直黑屏 / 停在菜单没反应？**
需要联网加载 Three.js 图形库。检查网络后刷新（`Ctrl+F5` 强制刷新）。

**点「进入园区」没锁定鼠标？**
个别浏览器需要再点一次页面；确保没有其他弹窗。`Esc` 后想继续，回菜单再点「进入园区」。

**画面卡顿？**
关闭其他标签页；浏览器硬件加速要开启（设置 → 系统 → 使用硬件加速）。

**掉进洞里 / 卡在方块里？**
按 `F` 开飞行飞出来；掉出世界会自动传送回出生点。

**「创建多人房间」显示组件未能加载？**
多人组件（PeerJS）需联网加载，且部分内网会拦截。刷新重试或换网络。

**找不到房间？**
确认代码输入正确（5 位、不分大小写）、房主页面还开着、双方都能上网。

**手机能玩吗？**
可以！支持触屏操作（左摇杆移动、右侧滑动转视角、按钮挖放方块）。建议横屏；大屏电脑体验仍然最佳。

---

## 10. 给老师的课堂建议

- **导览课（15–20 分钟）**：老师建房间投屏领队，学生加入跟随，沿 ①→⑥ 顺序走一遍，每到一个信息点让一位学生朗读讲解；
- **讨论题**：为什么入口不设门？圆形庭院解决了什么问题？绿色屋顶有哪些作用？矩形与圆形相交表达了什么？
- **建造任务**：分组在园区空地上为 Campus 设计「三期扩建」，要求延续放射状布局并至少使用一种绿色建筑元素，最后互相参观点评；
- **跨学科**：几何（圆、环、放射对称）、能源与环境（绿色建筑）、本地企业与工业园区认知（Batu Kawan）。

---

## 11. 技术说明

| 项目 | 说明 |
|---|---|
| 文件 | `minecraft/index.html` 单文件，约 1500 行，无构建步骤 |
| 3D 引擎 | [Three.js](https://threejs.org) r160（CDN 加载） |
| 世界 | 324×129×40 体素，分 16×16 区块合并网格渲染；纹理全部由 Canvas 程序化生成，无外部素材 |
| 建筑生成 | 按真实设计概念参数化生成：圆形庭院大草坪与中央白色圆坪、两层环形楼（与实景层数一致，四周为平整草坪）、屋顶环形光伏雨棚（钢梁 + 白柱架空）、庭院深灰色楼梯塔（折返楼梯连通每层，塔身高出屋面）、两栋矩形楼与体量相交、方块像素字 VITROX 招牌、底层员工食堂与洗碗棚、VITROX 招牌大楼四楼东半层两间电脑室（共约 20 台电脑，经食堂旁直跑楼梯上楼）、围栏篮球场（西侧）与两个停车场（入口东侧小型 + 西侧大型，含随机车辆）、东侧 Campus 3.0 扩建区（两栋光伏屋顶板楼、深灰楼梯塔、鱼池庭院与螺旋圆园）、紧邻 Campus 3.0 的 ViTrox Education 流线型飘带大楼（弧形玻璃立面、地面架空柱廊、入口雨棚）、实验楼四层 ViTrox 检测设备产线（TR/TH/V510 风格机台 + 室内楼梯）、环形走廊 ViTrox Gallery 贵宾展廊（蓝地毯、金奖杯、纪念品展示柜、公司历史相框墙，从入口一直通到食堂）、正门迎宾环岛（绿岛花丛 + 环形车道 + 透明玻璃雨棚与 ViTrox 绿墙）、门厅接待柜台与二楼玻璃连桥（横跨入口上空，连接环形楼两侧二层） |
| 多人 | [PeerJS](https://peerjs.com)（WebRTC DataChannel），星型拓扑：房主中继并保存世界，同步方块编辑与玩家位置（约 8 次/秒） |
| CodingMine | 虚构软件公司背景的 C/C++ 编程课程：共 10 章、23 道题、230 分，答对累计分数并存入 `localStorage`；三种语言均有对应题目内容；电脑室分隔墙上的黑板用 Canvas 贴图实时显示进度 |
| 存档 | `localStorage`（仅记录与原始世界的差异） |

---

*建筑原型版权归 CYC Architect / ViTrox 所有；本程序为教育用途的方块化演绎，非精确比例模型。*
